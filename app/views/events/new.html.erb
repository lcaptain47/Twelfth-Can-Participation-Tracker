<%= form_with(model: Event.new, local: true) do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name%>

    <%= f.label :date %>
    <%= f.date_select :date%>

    <%= f.submit %>
<% end %>

<%= link_to "Homepage", events_path %>